pipeline {
  agents any
  options {
    timeout(time: 1, unit: "SECONDS")
  }
  tools {
    maven 'Maven3.9'
  }
  stages{
    stage('Build'){
      steps{
        echo 'Hello World
      }
    }
  }
}
